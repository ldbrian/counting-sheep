# BUILD PLAN：《数羊》v0.1

## Goal

一个晚上陪你数羊的小玩意儿：你摇 → 羊从左边走到右边 → 你开始数 → 突然有一只羊折返 → 角落一行「今晚：121」永不解释。最后「我数完了」输入数字，系统只记录、不判断。

核心不是数数，是「数的过程」里那种「它不配合」的荒诞感。**系统永不判断对错。**

---

## Simplest Solution

单文件 `index.html`，纯 HTML + CSS + JS，零依赖，双击即开。

- **羊**：CSS 画的像素羊 / 或一个 canvas 精灵，从左边走到右边，走姿要耐看。
- **折返**：羊走到一半，改个方向走回去——用 JS 控制方向变量。
- **摇**：桌面端替代 = 鼠标快速左右甩动 或 按住左右摇 / 点屏幕边缘。监听 `pointermove` 的 `dx` 累计，超过阈值算一次「摇」。
- **计数**：角落一行 `今晚：N`，每次摇完 N 增加一个「永远不太对劲」的数字（不是羊的数量，是「今晚的第 N 只」，永不解释）。
- **「我数完了」**：一个按钮 → 弹一个输入框 → 用户输数字 → 系统回一句荒诞话（「27。好。今晚的第 121 只羊。」），只记录不判断。

没有后端、没有数据库、没有关卡、没有对错判定。

---

## Tech Decision

| 选择 | 理由 |
| --- | --- |
| 原生 HTML/CSS/JS，单文件 | 双击即开、零依赖，小玩意儿优先 |
| 无框架 | 状态就一个羊数组 + 一个计数，不需要 React |
| 羊走姿用 canvas 或 CSS 动画 | 只有「从一边走到另一边」一种移动，最简单即可 |
| 摇 = 鼠标左右快速甩动 | 复用现有 `pointermove`，不加任何库 |
| 无音效 v0.1 | IDEA 没要求音效，先让它出生（能加可后补） |

---

## Core States

```
idle（空，深蓝）──摇──▶ spawning（出 1~3 只羊）──▶ walking（羊从左到右）
walking ──随机──▶ turnaround（一只折返）──▶ walking
walking ──▶「我数完了」──▶ confess（输入数字）──▶ record（只记录，回荒诞话）
```

- `walking`：羊向右走，走姿循环。
- `turnaround`：某只羊停下、调头、往回走（v0.1 唯一「故意不对」）。
- `confess`：「我数完了」弹输入框。
- `record`：系统回一句话，不判断。

---

## Architecture（单文件内部结构）

```
<body>
  <main id="world">    ── 深蓝背景 + 羊的舞台（羊从 left 移到 right）
  <div id="counter">   ── 角落一行「今晚：N」
  <button id="done">   ──「我数完了」
  <div id="confess">   ── 输入框 + 一句回复
```

`<script>` 按序组织：
1. `state`：`sheep[]`（每只：x, speed, dir, state）、`tonight`（计数）、`lastShakeT`。
2. `spawnSheep(n)`：造 1~3 只羊，从左侧进入。
3. `moveSheep(dt)`：更新 x；随机触发 `turnaround`。
4. `pixelSheep()`：画一只像素羊（小函数，可复用）。
5. `render()`：重绘画面。
6. `shake()`：鼠标快速左右甩动检测。
7. `onDone()`：`confess` 流程。
8. `say()`：角落的荒诞计数 + 输入后的回复。

---

## 关键参数（Builder 直接照抄）

```
SHAKE_DIST  = 120      // 鼠标左右甩动的累计距离阈值，触发一次摇
SHAKE_COOLDOWN = 800   // 两次摇最小间隔 ms
SHEEP_N     = rand(1, 3)   // 每次摇出 1~3 只
SHEEP_SPEED = 30~50    // px/s，随机
WALK_LIFE   = 12000~22000 // 一只羊横穿时长
TURN_CHANCE = 0.18      // 每只羊每走完一定距离后折返的概率
TONIGHT_STEP = rand(1, 9)  // 每次摇后「今晚」增加一个任意数字（不是羊数）
COUNTER_BASE = 90       // 初始「今晚」基数（让它一开始就"不对劲"）
```

**核心不判断**：`onDone` 里用户输入什么数字都不校验，只存进 `tonight` 相关状态 + 回一句固定荒诞话。

---

## DON'T BUILD

- 后端 / 数据库 / 账号 / 登录
- 关卡 / 通关 / 输赢 / 对错判定 / 分数
- 计时器 / 限时 / 强制结算
- 助眠 / 睡眠产品定位（界面不放任何「助眠」「睡眠」字样）
- 设置页 / 进度条 / 完成度 UI
- 多页面 / 路由 / 框架 / 构建工具 / npm
- 图片、音频、字体文件（全部 CSS/JS 生成，用系统字体）
- 移动端真摇动（用桌面替代方案即可，不专门做）
- 「以后可能需要」的抽象

---

## Tasks

### Task 01 — 深蓝舞台 + 一只羊

- **目标**：第一眼成立——深蓝，一只羊从左边走到右边，走姿耐看。
- **输入**：IDEA 第一眼画面 + v0.1。
- **输出**：
  - 近乎黑的深蓝纯色背景。
  - 一只像素羊（画到「一眼知道是羊、有点可爱」），从左侧走进画面、匀速走到右侧，走姿有循环动画（腿/身体轻微摆动）。
  - 循环播放：一只走完再随机隔一会儿来下一只。
- **验收**：双击打开即可看到深蓝 + 一只羊横穿；走姿不突兀、一眼是羊；页面无标题无导航。

### Task 02 — 摇 = 召唤

- **目标**：摇一下，多几只羊。
- **输入**：IDEA 交互 + v0.1。
- **输出**：
  - 鼠标快速左右甩动（`SHAKE_DIST` 累计距离）→ 算一次摇，`SHAKE_COOLDOWN` 内不重复触发。
  - 每次摇随机出现 1~3 只羊，从左侧进入。
  - 角落出现「今晚：N」一行小字，每次摇后 N 增加 `TONIGHT_STEP`（一个「不对劲」的随机数字，不是羊的数量）。
- **验收**：左右甩鼠标能连出几只羊；角落数字每次随机增；羊会排队横穿。

### Task 03 — 折返（v0.1 唯一「故意不对」）

- **目标**：数着数着，一只羊突然往回走。
- **输入**：IDEA v0.1「羊折返」。
- **输出**：
  - 每只羊在行走中有概率折返：停下、调头、原路走回去（`TURN_CHANCE`）。
  - 折返后走回左侧边缘即消失。
  - 折返的瞬间要「有生命感」——停顿一下、可能回头看你一眼再走（视觉小停顿）。
- **验收**：观察若干只羊，能看到至少一次折返；折返停顿自然、像有想法；不打断其他羊。

### Task 04 — 「我数完了」与记录不判断

- **目标**：用户数完，系统只记录、不判断。
- **输入**：IDEA 交互「我数完了」+ 核心重定义。
- **输出**：
  - 一个「我数完了」按钮（或点击画面边缘）。
  - 点击后出现一个输入框，让用户输入数字。
  - 系统**不判断对错**，只回一句荒诞话并收场，例如：
    - 「27。好。今晚的第 121 只羊。」（用 `tonight` 的真实值拼一句）
    - 「原来如此。」然后画面缓缓安静。
  - 角落数字「今晚：N」从头到尾永不作解释。
- **验收**：输任何数字（包括明显错的）都不会收到任何「对/错」反馈；只收到荒诞回复；画面自然收场；深蓝安静。

### Task 05 — 安静收尾（体验弧线的最后一环）

- **目标**：从荒诞小游戏，变成「一段没有事情发生的时间」。
- **输入**：IDEA 体验弧线 4~5。
- **输出**：
  - 「我数完了」后：世界慢慢变少、变慢、变暗。
  - 剩余羊逐渐消失，不再有新的摇/羊；画面渐暗，只有深蓝 + 一行小字（如「今晚：N」）静静停留。
  - 不强制结束，不弹结算。
- **验收**：输入数字后画面约 10~20 秒内逐渐安静变暗，只剩角落数字；整个过程无提示语、无弹窗结算。

---

## 完成标准

按 Task 顺序做，每步验收通过再继续。全部完成：一个「你认真数、它不配合」的深蓝数羊之夜。摇 → 出羊 → 折返 → 数不清 → 我数完了 → 只记录不判断 → 世界安静下来。